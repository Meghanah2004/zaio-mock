# Dev/test-only tooling. NOT required to run the application - see
# requirements.txt for runtime dependencies.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
pytest>=9.1,<10
httpx>=0.28,<1
pip-audit==2.10.1
bandit==1.9.4
ruff==0.16.6
mypy==2.3.1
# Type stubs mypy needs to check src/rendering/pdf_renderer.py and
# src/validation/schema_validator.py (reportlab and jsonschema ship no
# inline type information of their own).
types-reportlab>=4.5,<5
types-jsonschema>=4.26,<5
